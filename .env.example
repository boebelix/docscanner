SCAN_DEVICE=fujitsu:fi-6130Zdj:404446
# SCAN_BUTTON=scan

# SCAN_UPLOADER=nfs              # nfs | smb | rsync | none  (comma-separated for multiple)
# SCAN_REMOTE_SHARE=/mnt/scans   # mount point for nfs / smb
# SCAN_RSYNC_TARGET=user@host:/scans

# SCAN_CONSUME_DIR=/mnt/paperless/consume  # Paperless-NGX consume folder (for consume_sync)
# SCAN_SYNC_INTERVAL=30                    # seconds between sync runs (default: 30)

# SCAN_QUEUE_DIR=
# SCAN_TEMP_DIR=
# SCAN_LOG_FILE=
